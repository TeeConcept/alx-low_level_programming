#include "main.h"
/**
 * _abs - Check main
 * Description: Computes the absolute integer value
 * @n: An integer input
 * Return: The absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (n * -1);
}
